DOC = doc
PROG = pdflatex
FLAGS =

export SOURCE_DATE_EPOCH = 1

one:
	texfot $(PROG) $(FLAGS) $(DOC).tex

two:
	texfot $(PROG) $(FLAGS) $(DOC).tex
	texfot $(PROG) $(FLAGS) $(DOC).tex

clean:
	rm -f $(DOC).aux $(DOC).log

.SILENT: one two
